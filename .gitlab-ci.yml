image: "alpine"

stages:
  - test

test-update:
  stage: test
  script:
    # Prepare script
    - mkdir -p ~/.steam/compatibilitytools.d/
    - chmod a+x ./update-proton-ge

    # Run to test script
    - ./update-proton-ge
